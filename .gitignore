# Rust build output
/target
/acs/target
/milepost/target

# milepost is a library crate; its standalone lockfile is not tracked
# (acs/Cargo.lock pins its dependencies for the shipped binary)
/milepost/Cargo.lock

# Electron / Node
node_modules/
/dist

# Electron app build artifacts (copied frontend)
electron/packages/app/out/

# Claude Code
.claude/settings.local.json

# starterpack — these paths must be tracked by git
!.beads/
!.beads/**
!.starterpack/
!.starterpack/**
!.claude/
!.claude/**
!.github/
!.github/**
!CLAUDE.md
!.gitattributes
.claude/scheduled_tasks.lock
